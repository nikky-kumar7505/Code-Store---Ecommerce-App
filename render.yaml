# Blueprint option A — deploy only the API from /server (recommended for this monorepo)
# https://render.com/docs/blueprint-spec
services:
  - type: web
    name: code-store-api
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "20"
